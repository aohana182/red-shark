faster-whisper
sounddevice
pywin32
pystray
pytest
ruff

# No Python LLM binding needed for cleanup: cleanup.py runs the official
# llama-server.exe binary (fetched by scripts/download_models.py, not pip)
# as a subprocess and talks to it over local HTTP. See dictate/config.py
# for why -- llama-cpp-python has no Python 3.14 wheel here, and the one
# community wheel that does crashes at model load on this CPU.
